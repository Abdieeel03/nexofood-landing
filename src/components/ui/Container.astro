---
interface Props {
  class?: string;
  id?: string;
}

const { class: className = '', id } = Astro.props;
---

<div id={id} class={`w-full max-w-7xl mx-auto px-4 md:px-6 ${className}`}>
  <slot />
</div>
